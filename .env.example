# Environment variables for Docker Compose
# Copy this file to .env and customize the values as needed

# Authentication token for API access
# Change this to a secure token in production
BEARER_TOKEN=secure-default-token-change-in-production

# MySQL database configuration (optional overrides)
# MYSQL_ROOT_PASSWORD=root_password
# MYSQL_DATABASE=notifications
# MYSQL_USER=notif_user
# MYSQL_PASSWORD=notif_pass

# Application configuration (optional overrides)
# IMAGE_STORAGE_PATH=/app/images
# JAVA_OPTS=-Xmx512m -Xms256m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0